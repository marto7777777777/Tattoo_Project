# InkRoute landing media

This directory intentionally contains no fake screenshots or stock product UI.

Add the real recordings and subtitle tracks here, then set their paths in
`src/landing/landingConfig.js`. Poster placeholders are already named in that
registry, so they can be added without changing the page components.
