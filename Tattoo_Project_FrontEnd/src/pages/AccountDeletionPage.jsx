import {useState} from "react";import {deleteAccount} from "../api/accountApi";import {useAuth} from "../context/AuthContext";import {useLanguage} from "../i18n/LanguageContext";
export default function AccountDeletionPage(){const{isLoggedIn,logout}=useAuth();const{language}=useLanguage();const bg=language==="bg";const[password,setPassword]=useState("");const[confirmation,setConfirmation]=useState("");const[error,setError]=useState("");const[done,setDone]=useState(false);async function submit(e){e.preventDefault();if(!window.confirm(bg?"Наистина ли искаш да изтриеш акаунта и свързаните лични данни?":"Delete your account and related personal data?"))return;try{await deleteAccount(password,confirmation);logout();setDone(true)}catch(err){setError(err.message)}}return <main className="legal-shell"><article className="legal-card"><h1>{bg?"Изтриване на акаунт и лични данни":"Account and personal data deletion"}</h1><p>{bg?"Можеш да изтриеш акаунта от тази страница или от Profile Settings. Активният Stripe абонамент ще бъде прекратен. Данни се запазват само когато законът задължително го изисква.":"You can delete your account here or from Profile Settings. An active Stripe subscription will be cancelled. Data is retained only where legally required."}</p><p>{bg?"След изтриване част от данните може да бъде запазена само когато това е необходимо по закон, за сигурност или за установяване, упражняване или защита на правни претенции.":"After deletion, limited data may be retained only where required by law, for security, or to establish, exercise or defend legal claims."}</p>{done?<p>{bg?"Акаунтът е изтрит.":"Your account has been deleted."}</p>:isLoggedIn?<form className="form" onSubmit={submit}><label>{bg?"Парола":"Password"}<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><label>{bg?"Напиши DELETE за потвърждение":"Type DELETE to confirm"}<input value={confirmation} onChange={e=>setConfirmation(e.target.value)} required/></label><button className="danger-button" type="submit">{bg?"Изтрий окончателно акаунта":"Permanently delete account"}</button>{error&&<p className="error">{error}</p>}</form>:<p>{bg?"Влез в акаунта си, за да продължиш.":"Log in to continue."}</p>}</article></main>}
